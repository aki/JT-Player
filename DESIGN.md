# JT Player（静听）— 设计说明

## 产品定位
本地 HiFi 音乐播放器（桌面），品牌名 **JT Player / 静听**。  
任务：把本地主流音频文件播起来，并以「录音室机架设备」的气质呈现播放状态。

参考图：`1.png` — 深色机箱、红色高亮、青色仪表读数、黑胶/CD 舱、双 VU、频谱、波形进度。

## 视觉方向

### Style anchor
Mastering studio rack gear / 高端 CD 播放器面板：设备分区、细边框、大写字母仪表标签、等宽遥测数据，而不是音乐 App 的圆角卡片流。

### Palette
| Token | Hex | 用途 |
|-------|-----|------|
| `--bg` | `#090A0C` | 全局底 |
| `--panel` | `#12141A` | 设备面板 |
| `--panel-2` | `#1A1D24` | 次级凹槽/表头 |
| `--line` | `#2A2E38` | 分割线、边框 |
| `--ink` | `#ECEAE6` | 主文字 |
| `--muted` | `#8B909A` | 次要信息 |
| `--accent` | `#D42B3A` | 播放高亮 / 徽章 / 当前曲 |
| `--signal` | `#3DDBD9` | 技术读数、活动态、进度头 |
| `--gold` | `#D4A84B` | HiFi 规格徽章 |
| `--ok` | `#3ECF8E` | 无损/就绪指示 |

### Typography
- UI / 标题：`Segoe UI`, `Microsoft YaHei`, `PingFang SC`, `system-ui`
- 数据 / 时间 / 采样率：`Cascadia Code`, `Consolas`, `ui-monospace`, monospace
- 层级：曲名 18–22px/600；面板标签 10–11px 大写 + 字距；遥测 12–13px 等宽

### Layout
```
┌──────────────── status strip ─────────────────┐
│ 左：播放列表     │ 中：封面+歌词+VU+波形 │ 右：碟舱/元数据/VU/FFT │
├──────────────── transport bar ───────────────┤
│ 音量 │ DSP/均衡 │ 曲号/时间/规格 │ 上一首 播放 下一首 │
└──────────────────────────────────────────────┘
```
- 三栏：左约 300–340px 播放列表；中弹性主舞台；右约 300–340px 仪表区
- 底栏通栏，固定高度约 64–72px
- 间距节奏：4 / 8 / 12 / 16 / 24；面板内边距 12–16px
- 密度偏高，像设备面板，不像阅读页

### Signature moments
1. **双 VU（横向电平条）** 位于封面下方，播放时随峰值跳动，带 dB 刻度
2. **波形进度** 全宽双声道形态，已播部分染青色，播放头为信号色竖线
3. **右侧模拟 VU 表盘 + 32 段 RTA 频谱** — 设备感的视觉锚点
4. **黑胶/CD 舱** 播放时碟面旋转（`prefers-reduced-motion` 时停）

### Risk
不用圆角卡片流、不用大图营销版式。签名元素集中在仪表，其余保持克制。

## 功能范围（v1）

### 必须
- 打开本地文件 / 文件夹，拖拽加入列表
- 主流格式（Chromium 解码）：`.mp3` `.wav` `.flac` `.ogg` `.oga` `.m4a` `.aac` `.webm` `.opus` `.mp4`
- 播放 / 暂停 / 上一首 / 下一首 / 进度拖拽 / 音量
- 播放列表：显示序号、曲名、艺术家、时长；双击播放；当前曲高亮
- 随机、循环（列表/单曲/关）、清空、添加
- 元数据：曲名、艺术家、专辑、格式标签（尽量从标签读取）
- 可视化：L/R VU、FFT 频谱、波形进度
- 键盘：`Space` 播放/暂停，`←/→` 快退快进，`↑/↓` 音量

### 可后续
- 均衡器真实 DSP、歌词文件（.lrc）解析、封面网络检索、APE/DSD/ffmpeg 外挂解码、媒体库扫描

## 技术选型
- **Electron** 桌面壳 + 原生 HTML/CSS/JS 渲染
- `<audio>` + Web Audio `MediaElementAudioSourceNode` + `AnalyserNode`
- `music-metadata`（主进程）读标签与封面
- 渲染层不依赖构建工具，便于直接预览

## 文件格式支持说明
Electron/Chromium 原生可解：MP3 / WAV / FLAC / OGG Vorbis / Opus / AAC / M4A / WebM。  
WMA、APE、DSD 等 v1 不支持，列表会标记为不可播放并跳过。
